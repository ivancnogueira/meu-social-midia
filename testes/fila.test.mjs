import test from 'node:test';import assert from 'node:assert/strict';import { mkdtemp,rm } from 'node:fs/promises';import { tmpdir } from 'node:os';import { join } from 'node:path';import { alterarJob,criarJob,lerJob } from '../automacoes/lib/fila.mjs';
test('aprovação é explícita e idempotente',async()=>{const raiz=await mkdtemp(join(tmpdir(),'sms-fila-'));try{await criarJob(raiz,{id:'TESTE-001',legenda:'Teste',imagens:['x']});await alterarJob(raiz,'TESTE-001','123','aprovar');assert.equal((await lerJob(raiz,'TESTE-001')).status,'aprovado');await assert.rejects(()=>alterarJob(raiz,'TESTE-001','123','aprovar'),/não está pronta/);}finally{await rm(raiz,{recursive:true,force:true});}});
