import test from 'node:test';import assert from 'node:assert/strict';import { publicarNaMeta } from '../automacoes/lib/meta.mjs';
test('Meta usa bearer nos GETs e devolve permalink',async()=>{const chamadas=[];let posts=0;const fetchFn=async(url,opcoes={})=>{chamadas.push({url,opcoes});if(opcoes.method==='POST'){posts++;return{ok:true,json:async()=>({id:`id-${posts}`})};}if(url.includes('status_code'))return{ok:true,json:async()=>({status_code:'FINISHED'})};return{ok:true,json:async()=>({id:'media',permalink:'https://instagram.example/post'})};};const r=await publicarNaMeta({fetchFn,apiVersion:'v-test',instagramId:'ig',token:'segredo-teste',urls:['https://img.example/a.png'],legenda:'Legenda'});assert.equal(r.permalink,'https://instagram.example/post');assert.ok(chamadas.filter(c=>!c.opcoes.method).every(c=>!c.url.includes('segredo-teste')&&c.opcoes.headers.authorization==='Bearer segredo-teste'));});
