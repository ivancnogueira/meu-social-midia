import { createServer } from 'node:http';
import { readFile, stat } from 'node:fs/promises';
import { extname, join, normalize } from 'node:path';
const raiz=process.cwd();const porta=Number(process.env.PREVIEW_PORT)||4173;const tipos={'.html':'text/html; charset=utf-8','.png':'image/png','.jpg':'image/jpeg','.jpeg':'image/jpeg','.webp':'image/webp','.css':'text/css; charset=utf-8','.js':'text/javascript; charset=utf-8'};
createServer(async(req,res)=>{try{const url=new URL(req.url,'http://local');if(url.pathname==='/'){res.writeHead(302,{location:'/previas/index.html'});res.end();return;}const pedido=decodeURIComponent(url.pathname);const caminho=normalize(join(raiz,pedido));if(!caminho.startsWith(normalize(raiz)))throw new Error('Caminho recusado');const info=await stat(caminho);if(!info.isFile())throw new Error('Não encontrado');res.writeHead(200,{'content-type':tipos[extname(caminho).toLowerCase()]||'application/octet-stream','cache-control':'no-store'});res.end(await readFile(caminho));}catch{res.writeHead(404,{'content-type':'text/plain; charset=utf-8'});res.end('Não encontrado');}}).listen(porta,'127.0.0.1',()=>console.log(`Prévias disponíveis em http://127.0.0.1:${porta}`));
